```js
/*
    initialize
        validate origin and provided methods

    validate url

    if url is valid
        parse the redirect url

        if redirect doesn't have `accessToken`
            fetchAccessTokens

        -> access token is now available
        -> format accessToken/refreshToken to match a certain schema

        return accessToken and refreshToken
*/
```
